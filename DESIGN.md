# 부동산(아파트) 가치 분석 앱 — 설계도 (v0.1)

> 아파트를 ① 가격비교(시점별 금액·변화율%), ② 현재가치, ③ 미래가치(입지·학군·교통·호재)로
> 분석하고 **하나의 종합 점수(0~100)**로 표현하는 앱.

---

## 1. 확정된 방향 (Decisions)

| 항목 | 결정 | 비고 |
|---|---|---|
| **데이터 출처** | 국토부 실거래가 공공데이터 API | 합법·안정적, 정식 서비스 재배포 부담 적음 |
| **지역 범위 (초기)** | 서울/수도권 전체 | 이후 전국 확장 |
| **사용자/플랫폼** | 정식 서비스 (불특정 다수, 웹) | 처음부터 DB·인증·확장 고려 |
| **미래가치 데이터** | 하이브리드 | 자동 가능(교통/입지)은 자동, 호재는 수동입력 |
| **기술 스택** | Python+FastAPI / PostgreSQL / Next.js | 아래 4장 |

### ⚠️ 알고 시작하는 한계
- 국토부 API는 **실거래가만** 제공 → **호가(현재 매물가)는 없음**. "실시간 매도호가 대비" 분석은 초기 미지원.
- 실거래 기반이라 **1~2주 신고 시차** 존재.

---

## 2. 핵심 개념 — 3개 점수 → 1개 종합점수

```
종합점수 (0~100)
├── 💰 가격 매력도 점수   ← 국토부 실거래가 (시계열·%)  [100% 자동]
├── 🎯 현재가치 점수      ← 유사단지 대비 상대평가       [자동 계산]
└── 🚀 미래가치 점수      ← 입지·학군·교통·호재          [하이브리드]
```

종합점수 = `w1·가격매력도 + w2·현재가치 + w3·미래가치` (가중치 w는 설정 가능, 초기 제안 40/30/30)

---

## 3. 각 점수 계산 로직

### 💰 가격 매력도 (국토부 API만으로 자동)
| 지표 | 의미 | 계산 |
|---|---|---|
| 전고점 대비 현재가 % | 지금 싼가? | (현재 평단가 − 전고점 평단가) / 전고점 |
| 기간별 변화율 | 추세 | 1M / 3M / 6M / 1Y 평단가 변화율 |
| 거래량 추세 | 매수세 강도 | 최근 거래건수 vs 과거 평균 |

- 평형(전용면적)별로 구분 계산. 단지 대표값은 주력 평형 기준.

### 🎯 현재가치 (상대 평가)
- 동일 생활권 **유사 단지군의 평단가 분포** 대비 이 단지의 위치(백분위)
- 하락장 방어력: 전고점 회복률, 변동성

### 🚀 미래가치 (하이브리드)
| 요인 | 방식 | 데이터 출처 |
|---|---|---|
| 교통 | 🤖 자동 | 지하철역 좌표 → 최단거리 (공공데이터/좌표) |
| 입지 | 🤖 자동 | 좌표 기반 (중심지 접근성 등) |
| 학군 | 🤖 반자동 | 학교알리미 등 학교 데이터 |
| 호재(재건축·GTX·개발계획) | ✍️ 수동입력 | 관리자가 단지별 등록 |

---

## 4. 기술 스택 & 시스템 구조

| 영역 | 선택 | 이유 |
|---|---|---|
| 백엔드/파이프라인 | Python + FastAPI | 데이터 수집·점수계산 최적 |
| DB | PostgreSQL | 실거래 시계열 대량 저장 |
| 프론트 | Next.js (React) | 정식 서비스·차트·SEO |
| 배포 | Docker | 확장 대비 |

```
[국토부 실거래가 API]
        │ 매일 배치 수집
        ▼
[데이터 파이프라인] ──► [PostgreSQL] (실거래 시계열)
                              │
                       [점수 엔진 Scoring]
                              │
                     [FastAPI 백엔드] ──► [Next.js 프론트] ──► [사용자]
```

### 데이터 모델 (초안)
- `complexes` — 단지 (id, 이름, 법정동, 좌표, 세대수 …)
- `transactions` — 실거래 (단지id, 전용면적, 층, 거래일, 거래금액 …)
- `poi` — 교통/학교 등 관심지점 (유형, 이름, 좌표)
- `catalysts` — 호재 (단지id/지역, 유형, 설명, 상태) *수동입력*
- `scores` — 산출 점수 스냅샷 (단지id, 점수유형, 값, 산출일)

---

## 5. 단계별 로드맵

- **Phase 1 (MVP)**: 국토부 수집 → DB 저장 → **가격 매력도 점수 + 시계열/% 차트** (수도권)
- **Phase 2**: 현재가치 (유사단지 비교)
- **Phase 3**: 미래가치 (교통 자동 + 호재 수동입력 관리화면)
- **Phase 4**: 종합점수 · 사용자 인증 · 정식 서비스화

---

## 6. 남은 디테일 (구현하며 확정)
- 종합점수 가중치 튜닝 기준
- 국토부 API 서비스키 발급 및 요청 한도
- 단지 마스터 데이터(단지id 매핑) 확보 방법
- 학군 점수 정의(배정 학교 vs 학원가 접근성)
